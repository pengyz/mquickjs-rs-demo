mode strict;

class User {
    fn getName() -> string;

    // any? param/return coverage on class methods
    fn echoAny(v: any?) -> any?;
}

singleton TestClass {
    fn makeUser(name: string) -> User;
}
