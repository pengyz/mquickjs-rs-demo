mode strict;

singleton AsyncTestSingleton {
    @nonCancellable
    fn nonCancellableTask(cb: callback(result: string)) -> void;
    
    @timeout(1000)
    fn timeoutTask(cb: callback(result: string)) -> void;
    
    fn cancellableTask(cb: callback(result: string)) -> void;
}
