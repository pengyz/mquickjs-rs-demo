module m1@1.0

class GcNode {
    opaque {
        held: Traced<i32>
        count: i32
    }

    fn getValue() -> i32;
}

class PlainNode {
    opaque {
        id: i32
    }

    fn getId() -> i32;
}
